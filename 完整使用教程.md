# Dify API 测试工具 - 完整使用教程

## 🎉 恭喜！您的测试工具已经成功运行

### 访问方式
- **完整项目版**: 打开浏览器访问 http://localhost:3000
- **单文件版**: 直接双击 `dify-api-tester-standalone.html`

## 📚 知识库 API 测试完整流程

### 第一步：配置 API 访问
1. 在页面顶部输入您的 **API Key**
2. 确认 **Base URL** 正确（默认为 https://api.dify.ai/v1）

### 第二步：创建知识库
1. 从左侧选择 "知识库 (Knowledge Base)" → "创建知识库"
2. 修改请求体中的参数：
```json
{
  "name": "我的测试知识库",
  "description": "用于测试的知识库",
  "permission": "only_me"
}
```
3. 点击"发送请求"
4. **重要**：记录返回的 `id` 字段（这是您的 dataset_id）

### 第三步：创建文档
1. 选择"通过文本创建文档"
2. 在请求体中添加 dataset_id：
```json
{
  "dataset_id": "您刚才获得的ID",
  "name": "测试文档",
  "text": "这是一个测试文档的内容。人工智能正在改变世界。",
  "indexing_technique": "high_quality",
  "process_rule": {
    "mode": "automatic"
  }
}
```
3. 发送请求，记录返回的 document_id

### 第四步：管理文档分段
1. 选择"添加文档分段"
2. 填写参数：
```json
{
  "dataset_id": "您的知识库ID",
  "document_id": "您的文档ID",
  "segments": [
    {
      "content": "这是第一个分段的内容",
      "answer": "这是分段的答案",
      "keywords": ["测试", "分段"]
    }
  ]
}
```

### 第五步：检索知识库
1. 选择"检索知识库"
2. 测试检索功能：
```json
{
  "dataset_id": "您的知识库ID",
  "query": "人工智能",
  "retrieval_model": {
    "search_method": "keyword_search",
    "reranking_enable": false,
    "top_k": 5,
    "score_threshold_enabled": false
  }
}
```

## 🤖 对话 API 测试流程

### 基础对话测试
1. 选择"对话与智能体" → "发送对话消息"
2. 默认参数已配置好，直接发送即可
3. 尝试修改 `response_mode` 为 "streaming" 测试流式响应

### 会话管理
1. 第一次对话时，`conversation_id` 留空
2. 系统会返回一个新的 conversation_id
3. 后续对话使用这个 ID 保持上下文

## 💡 高级技巧

### 1. 批量测试
您可以在历史记录中快速重放之前的请求

### 2. 路径参数处理
所有 `:参数名` 格式的路径参数都需要在请求体中提供：
- `:dataset_id` → 在请求体添加 `"dataset_id": "xxx"`
- `:document_id` → 在请求体添加 `"document_id": "xxx"`

### 3. 文件上传（完整版专属）
对于"通过文件创建文档"等 API，完整版支持直接上传文件

### 4. 导出配置
您可以复制请求历史，保存常用的 API 调用配置

## 🔧 常见问题解决

### Q: 遇到 CORS 错误？
A: 
1. 确认您的 Dify 服务器允许跨域访问
2. 或使用服务器端代理转发请求

### Q: API Key 无效？
A: 
1. 确认 API Key 正确复制（无多余空格）
2. 检查 API Key 是否有相应权限
3. 确认 Base URL 与您的 Dify 部署匹配

### Q: 流式响应不工作？
A: 
1. 确保 response_mode 设置为 "streaming"
2. 某些 API 不支持流式响应

## 📞 获取帮助

如需更多帮助：
1. 查看 Dify 官方文档：https://docs.dify.ai/
2. 访问项目 GitHub（如果开源）
3. 联系技术支持

祝您测试愉快！ 🚀 